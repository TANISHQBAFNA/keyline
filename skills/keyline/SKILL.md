---
name: keyline
description: Deprecated alias. Product is now Resolve. Use the resolve skill and npm run resolve.
---

# Keyline (renamed)

Product is **Resolve**. Tagline: **Figma rules. Agents resolve.**

Use `skills/resolve` and:

```bash
npm run resolve -- recommend "checkout with primary button"
npm run resolve -- resolve "Component Name"
```

`npm run keyline` still works as a deprecated script alias for this release.

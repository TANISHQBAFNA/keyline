{
  "version": 1,
  "howToAdd": "Copy this file to .graphify/context-packs.json. Fill in your product, the journey step (for example checkout summary), and who the screen is for. Bind recipeIds to recipes you already have — use the recipe id (checkout-summary), not the title. Never add Figma component ids; recommend fills those after ingest. Set active to the pack agents should load by default.",
  "active": "storefront-checkout-summary",
  "packs": [
    {
      "id": "storefront-checkout-summary",
      "product": { "id": "storefront", "name": "Storefront" },
      "domain": "checkout",
      "journey": { "step": "summary", "screenJob": "checkout summary" },
      "audience": "returning shopper",
      "constraints": { "density": "compact", "a11y": "wcag-aa" },
      "recipeIds": ["checkout-summary"],
      "libraryRules": { "deny": ["Banner"] }
    }
  ]
}
